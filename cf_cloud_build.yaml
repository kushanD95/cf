steps:
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: Deploy
    entrypoint: 'bash'
    args:
      - '-c'
      - |
          gcloud functions deploy deployCF \
          --runtime=go116 \
          --region=us-central1 \
          --entry-point=DeployCF \
          --source=. \
          --memory=256MB \
          --timeout=60s \
          --project=test-prj \
          --trigger-http \
          --topic= \
          --bucket= \
          --set-env-vars  \
          --set-secrets 

    timeout: '600s'